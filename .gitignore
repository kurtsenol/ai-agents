# Python
__pycache__/
*.pyc
.venv/

# Secrets — never commit keys. Use env vars or a local .env kept out of git.
.env
*.pem

# OS / editor noise
.DS_Store
*.db

debug.log
formatted_log.txt
runs.jsonl

# Internal — company data models, field names, real request examples.
# This repo is public; these must never be pushed.
segment-agent-plan.md
*.internal.md
internal/
